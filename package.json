{
  "name": "gpu-guru",
  "version": "1.0.0",
  "description": "Storefront dashboard to sell graphics cards",
  "default": "src/index.html",
  "scripts": {
    "clean": "rm -rf node_modules .git .parcel-cache dist .DS_Store",
    "static": "cp -rf ./src/static .dist",
    "start": "npx parcel --no-cache src/*.html",
    "build": "npx parcel build src/*.html"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/t-fisher98/gpu-guru.git"
  },
  "author": "Tyler Fisher",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/t-fisher98/gpu-guru/issues"
  },
  "homepage": "https://github.com/t-fisher98/gpu-guru#readme",
  "dependencies": {
    "firebase": "^9.6.6",
    "parcel": "^2.2.1"
  }
}
